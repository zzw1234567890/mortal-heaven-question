# Smoke Check Report — Sprint 13

> **Date**: 2026-09-11
> **Sprint**: Sprint 13（presentation-layer-complete 里程碑）
> **Engine**: Godot 4.6.3.stable
> **QA Plan**: `production/qa/qa-plan-sprint-13-2026-09-08.md`
> **Argument**: sprint

---

## Automated Tests

**Status**: **PASS**（2598 tests，2597 passing，1 pending，0 failing）

- 全量套件：152 scripts / 2598 tests（GUT 9.6.1，`godot --headless -s addons/gut/gut_cmdln.gd -ginclude_subdirs -gdir=res://tests -gexit`）
- 1 pending：save_load 多步迁移占位（CURRENT_SCHEMA_VERSION >= 2 待首次升级实现——预期设计）
- 1 failing 项在最终复跑中通过：realm `test_ac010_realm_up_triggers_gsm_realm_changed` 为**预存 flaky**（Sprint 12 起登记在案，间歇性失败/通过，非本冲刺引入——hud 004 全量基线时同样观察到该行为）
- CI：未配置（`.github/workflows/` 不存在）——测试为本地运行

## Test Coverage

| Story | Type | Test File | Coverage Status |
|-------|------|-----------|----------------|
| hud 001 挂载与可见性 | Integration | `tests/integration/hud/test_hud_scene_visibility.gd` | COVERED |
| hud 002 境界+修为条 | Logic+UI | `tests/unit/hud/test_cultivation_bar_state.gd` | COVERED |
| hud 003 灵石+卡组计数 | Logic+UI | `tests/unit/hud/test_lingshi_formatter.gd` + `test_deck_count_state.gd` | COVERED |
| hud 004 通知系统 | Logic+Integration | `tests/unit/hud/test_notification_stack.gd` + `tests/integration/hud/test_notification_request_interface.gd` | COVERED |
| hud 005 暂停菜单 | UI+Integration | `tests/integration/hud/test_pause_menu.gd` + `test_pause_combat_state_preserved.gd` | COVERED |
| R-01 双焦点 spike | Spike | spike 记录文档 | EXPECTED |

**Summary**: 5 covered（含全部 must-have story），0 manual 缺失，0 missing，1 expected（spike）。

## Manual Smoke Checks（用户批次验证）

- [x] 核心稳定性批：全部通过（无失败项选中）
- [x] HUD 四组件（本冲刺主体交付）：通过
- [x] 意外崩溃/报错：未观察到
- [x] 既有功能回归：无
- [-] 存档/读档：N/A——当前主场景尚未接入游戏启动流程（main-menu epic 未建），端到端存档周期无法在完整游戏流程中验证；SaveLoadSystem 单元/集成测试全绿覆盖存档链路，hud 005 保存并退出路径已由集成测试 mock 覆盖
- [-] 性能：F6 手动验证期间未观察到掉帧/卡顿（模糊动画流畅）；正式 Profiler 实测归 S13-12 R-03 D3D12 冒烟 spike

## QA 计划冒烟范围对照

| # | QA 计划关键路径 | 状态 |
|---|----------------|------|
| 1 | 游戏启动到主菜单无崩溃 | N/A——主菜单场景未创建（main-menu epic），当前验证载体为 HUD.tscn F6 |
| 2 | 开始新游戏（身份选择→初始地图） | N/A——同上，identity-select 未建 |
| 3 | HUD 在探索场景正确显示 | PASS——组件级测试 77 unit + 66 integration + F6 手动验证 |
| 4 | 战斗 HUD 隐藏+暂停可用 | PASS——`test_pause_combat_state_preserved.gd` 4 测试 + 可见性矩阵测试 |
| 5 | 通知系统滑入消失 | PASS——66 integration + 证据文档已签收 |
| 6 | 存档/读档周期 | N/A（见上）——链路级测试全绿 |
| 7 | 性能预算（帧尖峰） | 未实测——归 S13-12 spike；手动验证无可见卡顿 |

注：#1/#2/#6 的 N/A 源于表现层里程碑的定位——完整游戏流程（主菜单/身份选择）属 main-menu epic（Sprint 14 铺路目标），非本冲刺范围。

## Missing Test Evidence

所有 Logic/Integration 故事均有测试覆盖。无缺失项。

## Verdict: **PASS WITH WARNINGS**

- 自动化测试 PASS（0 确认失败）
- 全部手动冒烟批通过
- 警告项：
  1. 主流程端到端（启动→主菜单→新游戏→存档周期）因 main-menu epic 未建而 N/A——属 Sprint 14 范围
  2. 性能 Profiler 实测递延至 S13-12 R-03 spike
  3. CI 未配置（既有状态——非本冲刺引入）

**构建已就绪进行 QA 签收（S13-7）。**

QA 移交：`/team-qa sprint`——完整 QA 周期（测试用例执行、bug 分类、签收报告）。

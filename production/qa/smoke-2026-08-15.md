# Smoke Check Report

**Date**: 2026-08-15
**Sprint**: Sprint 3 — Core 层剩余系统 + 技术债
**Engine**: Godot 4.6.3
**QA Plan**: `production/qa/qa-plan-sprint-3-2026-08-10.md`
**Argument**: sprint

---

## Automated Tests

**Status**: PASS — 1145/1146 测试通过，0 失败，1 pending

- 1 pending（`test_migrate_if_needed_multi_step`——save_load 多步迁移，`CURRENT_SCHEMA_VERSION >= 2` 首版有意延后，与 Sprint 3 无关）
- 0 失败
- 1 orphan（`test_gsm_sync.gd`——input 系统遗留内存泄漏，非 Sprint 3 引入，见 Advisory）
- 62 测试脚本，运行 ~44s

测试命令：
```bash
godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests -gexit
```

### 本次冒烟检查期间额外修复的遗留测试缺陷（非 Sprint 3 引入）

在覆盖率扫描中发现 EventSystem 3 个测试文件存在 parse error，导致其中 28 个测试**从未被 GUT 加载运行**。本次一并修复（+28 测试解锁，全通过）：

| 文件 | parse error 根因 | 修复 |
|------|------------------|------|
| `test_event_trigger.gd` | 10 处 `var instance := es.trigger_event(...)`——`es` 为 `Node` 动态分派，`:=` 无法推断返回类型 | 改为 `var instance: EventInstance = ...` |
| `test_resolve_option.gd` | 8 处 `var results := es.resolve_option(...)`——同上 | 改为 `var results: Array = ...` |
| `test_select_event.gd` | 1 处续行 `+` 开头的卡方表达式被解析器当缩进错误 | 拆为 `heavy_term`/`light_term` 中间变量 |

EventSystem 套件修复后：13 scripts / 190 tests / 190 passing。

---

## Test Coverage

| Story | Type | Test File | Coverage Status |
|-------|------|-----------|----------------|
| 3-2 CostSystem 费用上限/恢复/临时加成 | Integration | `tests/integration/cost_system/test_cost_system_basic.gd` | COVERED |
| 3-3 双重信号路径 cost_changed + batch_updated | Logic | `tests/unit/cost_system/test_cost_signals.gd` | COVERED |
| 3-4 StatusEffect 8 阶段管线核心 | Integration | `tests/integration/status_effect/test_status_lifecycle.gd` | COVERED |
| 3-5 3 叠加规则 + 免疫 + 20 上限 | Logic | `tests/unit/status_effect/test_stacking_immunity.gd` | COVERED |
| 3-6 snapshot 导出 + 暂挂/恢复排序 | Integration | `tests/integration/status_effect/test_snapshot_suspend.gd` | COVERED |
| 3-7 SchoolSystem const + 纯查询接口 | Logic | `tests/unit/school_system/test_school_library_query.gd` | COVERED |
| 3-8 5 流派增益公式 + 不可驱散 | Logic | `tests/unit/school_system/test_school_effects.gd` | COVERED |
| 3-9 拆分 game_state_manager.gd | Refactor | 复用 `tests/unit/gsm/` + `tests/integration/gsm/` 6 测试文件 | COVERED |
| 3-10 GSM 第二层方法独立单测 | Task | `tests/unit/gsm/test_second_layer_methods.gd` | COVERED |
| 3-11 ADR-0003 文档补充 | Doc | 文档审查（ADR-0003 §visited_ids 生命周期章节） | MANUAL |
| 3-12 Feature 层预创建 | Planning | — | EXPECTED |

**Summary**: 8 covered（Logic/Integration/Refactor/Task）, 1 manual（Doc）, 1 expected（Planning）, 0 missing.

---

## Manual Smoke Checks

### 批处理 1 —— 核心稳定性

- [x] 项目启动无崩溃（Godot headless 模式，退出码 0）— PASS
- [x] Autoload 链初始化完整（GSM #1 → ... → StatusEffectSystem #10，10 个已注册）— PASS
- [~] 新游戏 / 主菜单输入响应 — 未在真实 GUI 环境逐项确认（headless 环境无法验证 GUI 交互；Core 层无玩家可见 UI，按 Sprint 2 先例 N/A）

### 批处理 2 —— Sprint 变更和回归

- [x] cost-system（费用上限/恢复/临时加成/双信号路径）— PASS（自动化覆盖）
- [x] status-effect（8 阶段管线/叠加/免疫/20 上限/snapshot）— PASS（自动化覆盖）
- [x] school-system（5 流派检测/增益公式/不可驱散）— PASS（自动化覆盖）
- [x] game_state_manager.gd 拆分（1016→282 行）零回归 — PASS（全量 1145 测试通过）

### 批处理 3 —— 数据完整性和性能

- [-] 存档/读档 — N/A（Core 层无玩家可见状态变更触发自动存档；SaveLoadSystem 已在 Sprint 1 实现，按 Sprint 2 先例未单独验证）
- [x] 性能 — PASS（Core 层无渲染循环；测试套件运行 ~44s 无卡顿）

---

## Missing Test Evidence

无。所有 Logic 和 Integration 故事均有测试覆盖。

---

## Advisory Notes（不阻塞，记录在案）

1. **1 orphan 测试** — `test_gsm_sync.gd`（input 系统）的 `test_ac006_no_own_signals_declared` 遗留一个未释放的 `input_manager.gd` Node 实例。input 系统为 Foundation 层（Sprint 1/2 范围），非 Sprint 3 引入，属既有内存泄漏 warning。
2. **RealmSystem(#11) / SchoolSystem(#19) 未注册 Autoload** — `project.godot` 的 `[autoload]` 段仅注册到 StatusEffectSystem（#10）。realm/school story 明确「本 Story 仅实现代码，Autoload 注册在项目配置阶段完成」，属预期架构决策，待 Feature 层（CombatSystem #20 接入时）一并注册。
3. **CardSystem 模板目录缺失** — `res://assets/cards/templates/` 不存在导致 CardSystem push_error「无法打开模板目录」。Core 层本无卡牌资产，属预期现象（Sprint 2 smoke 亦记录同类）。

---

## Verdict: PASS WITH WARNINGS

冒烟检查通过，构建已准备好进行手动 QA。

- 自动化测试：1145/1146 通过，0 失败
- 手动冒烟检查（批处理 1/2/3）PASS 或 N/A（核心 GUI 交互项 headless 环境无法逐项确认）
- 无 MISSING 测试证据条目
- Advisory 项均为 S3/S4 或预期架构决策，不阻塞

**QA 移交**：将 `production/qa/qa-plan-sprint-3-2026-08-10.md` 共享给 qa-tester 智能体以开始手动验证。

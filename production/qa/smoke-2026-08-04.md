# Smoke Check Report

**Date**: 2026-08-04
**Sprint**: Sprint 1 — Foundation 层
**Engine**: Godot 4.6.3
**QA Plan**: `production/qa/qa-plan-sprint-1-2026-07-27.md`
**Argument**: sprint

---

## Automated Tests

**Status**: PASS（521 tests, 520 passing, 1 pending）

- 测试框架：GUT（Godot Unit Test）34 个脚本
- 总测试数：521
- 通过：520
- 失败：0
- Pending/Risky：1（`tests/unit/save_load/test_migration_chain.gd::test_migrate_if_needed_multi_step` — 多步迁移待 CURRENT_SCHEMA_VERSION >= 2 时实现，既有待实现项，非本 Sprint 引入）
- 断言数：1763
- 运行时间：15.783s
- Orphans：1（`test_gsm_sync.gd::test_ac006_no_own_signals_declared` 中 InputManager Autoload 实例，非内存泄漏）
- 命令：`godot --headless -s addons/gut/gut_cmdln.gd -gconfig=.gutconfig.json`

---

## Test Coverage

Sprint 1 实际 23 个 Story 全部有测试覆盖：

| # | Epic/Story | 类型 | 测试文件 | 覆盖状态 |
|:--|:--|:--|:--|:--|
| 1 | gsm/001 三层 API 基础 | Logic | `tests/unit/gsm/autoload_and_tier1_read_test.gd` | COVERED |
| 2 | gsm/002 第二层原子写入 | Logic | `tests/unit/gsm/atomic_write_methods_test.gd` | COVERED |
| 3 | gsm/003 信号订阅 + batch_updated | Logic | `tests/unit/gsm/signal_layer_and_batch_updated_test.gd` | COVERED |
| 4 | gsm/004 序列化与反序列化 | Logic | `tests/unit/gsm/serialize_deserialize_test.gd` | COVERED |
| 5 | gsm/005 校验跳过 + enable_validation | Integration | `tests/integration/gsm/validation_skip_and_enable_test.gd` | COVERED |
| 6 | input/001 四级锁栈 | Logic | `tests/unit/input/test_lock_stack.gd` | COVERED |
| 7 | input/002 双焦点判定 | Logic | `tests/unit/input/test_input_judgment.gd` | COVERED |
| 8 | input/003 GSM 同步信号传播 | Integration | `tests/unit/input/test_gsm_sync.gd` | COVERED |
| 9 | input/004 MODAL 覆盖边缘 | Integration | `tests/unit/input/test_modal_override.gd` + `test_device_mask_edge_cases.gd` + `test_lock_leak_prevention.gd` | COVERED |
| 10 | scene/001 5 阶段管线 | Logic | `tests/unit/scene_manager/test_scene_manager_pipeline.gd` + `test_scene_manager_validation.gd` | COVERED |
| 11 | scene/002 TransitionType + 音频矩阵 | Logic | `tests/unit/scene_manager/test_transition_type.gd` + `test_audio_transition_matrix.gd` | COVERED |
| 12 | scene/003 自动存档 + 输入锁 | Integration | `tests/integration/scene_manager/test_auto_save_integration.gd` + `test_input_lock_integration.gd` | COVERED |
| 13 | scene/004 加载画面 + 异步 + 错误恢复 | Integration | `tests/integration/scene_manager/test_loading_screen.gd` + `test_error_recovery_integration.gd` | COVERED |
| 14 | save/001 JSON 引擎 + 枚举 | Logic | `tests/integration/save_load/test_json_engine.gd` | COVERED |
| 15 | save/002 原子双写 + Windows 重试 | Integration | `tests/unit/save_load/test_atomic_write.gd` | COVERED |
| 16 | save/003 容器 schema + 校验 | Integration | `tests/unit/save_load/test_container_schema.gd` | COVERED |
| 17 | save/004 公共 API + GSM 集成 | Integration | `tests/unit/save_load/test_public_api.gd` | COVERED |
| 18 | save/005 迁移链 + VERSION_MISMATCH | Logic | `tests/unit/save_load/test_migration_chain.gd` | COVERED |
| 19 | event/001 EventTemplate Resource | Logic | `tests/unit/event_system/test_event_template.gd` | COVERED |
| 20 | event/002 EventInstance + 触发/判定/结算 | Logic | `tests/unit/event_system/test_event_instance.gd` + `test_event_trigger.gd` + `test_resolve_option.gd` + `test_select_event.gd` + `test_check_condition.gd` + `test_load_templates.gd` | COVERED |
| 21 | event/003 story_flags 写入契约 | Logic+Integration | `tests/unit/event_system/test_set_flag.gd` + `test_get_flag.gd` + `tests/unit/gsm/test_set_narrative_flag.gd` + `tests/integration/event_system/test_story_flags_delegation.gd` | COVERED |
| 22 | event/004 连锁事件 + 循环检测 | Logic | `tests/unit/event_system/test_chain_event_depth.gd` + `test_chain_event_cycle.gd` + `test_chain_event_option_filter.gd` | COVERED |
| 23 | event/005 结果执行器 + ADD_CARD 委托 | Integration | `tests/unit/event_system/test_apply_outcomes.gd` + `tests/integration/event_system/test_card_reward_delegation.gd` + `test_full_event_flow.gd` | COVERED |

**Summary**: 23 covered, 0 manual, 0 missing, 0 expected.

---

## Manual Smoke Checks

Foundation 层为纯基础设施层（5 个 Autoload，无面向玩家 UI），手动冒烟检查项由自动化测试 + headless 启动验证 + grep 扫描替代覆盖：

- [x] 项目启动无崩溃 — PASS（`godot --headless --quit-after 5` 运行仅输出引擎版本行，无错误）
- [x] 5 个 Autoload 按序初始化 — PASS（`tests/integration/autoload/autoload_init_order_test.gd` + `autoload_init_timing_test.gd` 覆盖）
- [x] GSM 三层 API 端到端 — PASS（4 个 GSM 单元测试文件 + 1 个集成测试文件覆盖）
- [x] 输入锁栈阻塞/释放 — PASS（`tests/unit/input/test_lock_stack.gd` 等 6 个测试文件覆盖）
- [x] 场景转换 5 阶段管线 — PASS（`tests/integration/scene_manager/` 4 个文件覆盖）
- [x] 存档写入/读取往返一致 — PASS（`tests/unit/save_load/` 4 个文件 + `tests/integration/save_load/` 1 个文件覆盖）
- [x] 事件模板解析/结果结算 — PASS（`tests/unit/event_system/` 11 个文件 + `tests/integration/event_system/` 3 个文件覆盖）
- [x] grep `change_scene_to_file\|change_scene_to_packed` 仅 SceneManager — PASS（仅 `src/ui/loading/loading_screen.gd:30` 文档注释提及，无实际代码调用）
- [x] grep `JSON.parse_string` 零实际调用 — PASS（仅 `src/foundation/save_load_system.gd:10,71` 文档注释作为禁止反例提及）
- [-] 内存 <500MB — 未手动检查（Foundation 层 5 个 Autoload 预计远低于预算，建议 QA 周期中在编辑器确认）

---

## Missing Test Evidence

无。所有 Logic 和 Integration 故事均有测试覆盖。

---

## Verdict: PASS

**判定依据**：
- 自动化测试 PASS（520/521 通过，1 pending 为既有待实现项，零失败）
- 所有批次手动冒烟检查 PASS 或 N/A（Foundation 层无 UI，由自动化测试替代覆盖）
- 无 MISSING 测试证据
- 两个 QA 计划要求的 grep 扫描均通过

---

## 关卡裁定

冒烟检查干净通过。构建已准备好进行手动 QA。

**QA 移交**：将 `production/qa/qa-plan-sprint-1-2026-07-27.md` 共享给 qa-tester 智能体以开始手动验证（下一步 `/team-qa sprint`）。

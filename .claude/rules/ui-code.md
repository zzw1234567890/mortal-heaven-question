---
paths:
  - "src/ui/**"
---


# UI 代码规则 (UI Code Rules)

- UI **绝不能**拥有或直接修改游戏状态 —— 仅用于显示，使用 commands/events 请求更改
- 所有 UI 文本必须通过本地化系统 —— 不得有硬编码的用户面向字符串
- 所有交互元素必须同时支持键盘/鼠标和手柄输入
- 所有动画必须可跳过并尊重用户的动态/无障碍偏好设置
- UI 声音通过音频事件系统触发，而非直接播放
- UI 绝不能阻塞游戏线程
- 可缩放文本和色盲模式是强制性的，而非可选的
- 在最小和最大支持分辨率下测试所有屏幕

# 交互模式库 (Interaction Pattern Library)

> **状态 (Status)**：活跃
> **创建日期 (Created)**：2026-07-27
> **最后更新 (Last Updated)**：2026-09-08
> **来源规范 (Source Specs)**：`design/ux/main-menu.md`、`design/ux/hud.md`、`design/ux/pause-menu.md`、`design/art/art-bible.md` §7

---

## 概述 (Overview)

本库记录「仙途问道」中所有可复用的交互模式。模式按类别组织，定义一致的行为、视觉表现和可访问性需求。每个 UX 规范通过模式名称引用本库，而非在每个屏幕中重新定义组件行为。

**设计原则**：
- 模式定义**行为**而非视觉——视觉由美术圣经 §7 驱动
- 模式从现有 UX 规范中**提取**——不凭空发明
- 新屏幕中发现的模式**追加**到本库——永不重复定义

---

## 模式目录 (Pattern Catalog)

| 类别 (Category) | 模式 (Pattern) | 使用屏幕 (Used In) |
|------|---------------|------|
| 按钮 (Button) | 主要按钮 (Primary Button) | 主菜单、暂停菜单 |
| 按钮 (Button) | 次要按钮 (Secondary Button) | 主菜单、暂停菜单、HUD |
| 按钮 (Button) | 危险按钮 (Danger Button) | 暂停菜单 |
| 按钮 (Button) | 图标按钮 (Icon Button) | HUD（暂停入口、战斗日志） |
| 面板 (Panel) | 面板 (Panel) | 所有屏幕 |
| 面板 (Panel) | 覆盖层 (Overlay) | 暂停菜单、设置、卡组查看 |
| 面板 (Panel) | 遮罩层 (Mask Layer) | 暂停菜单、过渡提示 |
| 对话框 (Dialog) | 确认对话框 (Confirmation Dialog) | 主菜单、暂停菜单 |
| 对话框 (Dialog) | 错误提示 (Error Alert) | 暂停菜单 |
| 输入 (Input) | 滑块 (Slider) | 设置（音量） |
| 输入 (Input) | 切换开关 (Toggle) | 设置（减少动态、全屏） |
| 输入 (Input) | 下拉菜单 (Dropdown) | 设置（分辨率、帧率、质量、语言） |
| 数据显示 (Data Display) | 进度条 (Progress Bar) | HUD（修为、行动力、HP） |
| 数据显示 (Data Display) | 状态图标+计数器 (Status Icon + Counter) | HUD（灵石、卡组、牌库、弃牌堆） |
| 数据显示 (Data Display) | 静态文本 (Static Text) | 所有屏幕（标题、版本号、进度文本） |
| 数据显示 (Data Display) | 悬停提示 (Tooltip) | HUD（修为详情、地图悬停） |
| 通知 (Notification) | 通知堆叠 (Notification Stack) | HUD |
| 导航 (Navigation) | 选项卡导航 (Tab Navigation) | 主菜单、暂停菜单、设置 |
| 导航 (Navigation) | 焦点环 (Focus Ring) | 所有屏幕 |
| 布局 (Layout) | 分隔线 (Divider) | 暂停菜单 |
| 布局 (Layout) | 按钮组 (Button Group) | 暂停菜单 |
| 动画 (Animation) | 过渡覆盖层 (Transition Overlay) | HUD（场景切换） |
| 游戏专用 (Game-Specific) | 手牌布局 (Card Hand Layout) | 战斗 HUD |
| 游戏专用 (Game-Specific) | 回合阶段指示器 (Turn Phase Indicator) | 战斗 HUD |
| 游戏专用 (Game-Specific) | 角色状态卡 (Character Status Card) | 战斗 HUD |
| 游戏专用 (Game-Specific) | 阵法槽位 (Formation Slot) | 战斗 HUD |
| 游戏专用 (Game-Specific) | 备战阵位预览 (Deployment Preview Grid) | 战斗（阶段 0） |
| 游戏专用 (Game-Specific) | 攻击目标选择 (Attack Target Selection) | 战斗（阶段 3） |
| 游戏专用 (Game-Specific) | 战利品选择网格 (Loot Selection Grid) | 战斗结算（胜利） |
| 游戏专用 (Game-Specific) | 节点图节点 (Node Graph Node) | 探索 UI |
| 游戏专用 (Game-Specific) | 路径连线 (Path Connection) | 探索 UI |
| 游戏专用 (Game-Specific) | 迷雾遮罩 (Fog of War Overlay) | 探索 UI |
| 游戏专用 (Game-Specific) | 画布缩放平移 (Canvas Zoom & Pan) | 探索 UI |
| 游戏专用 (Game-Specific) | 结算面板 (Summary Panel) | 探索（结束/通关）、战斗（战败） |
| 游戏专用 (Game-Specific) | 地图选择卡片 (Map Selection Card) | 地图选择 |
| 游戏专用 (Game-Specific) | 商品卡 (Shop Item Card) | 坊市（买卡） |
| 游戏专用 (Game-Specific) | 散功选择网格 (Deck Removal Grid) | 坊市（散功/售卡） |
| 游戏专用 (Game-Specific) | 超限弃牌网格 (Overlimit Discard Grid) | 超限弃牌 |

---

## 模式定义 (Patterns)

### 按钮 (Button)

#### 主要按钮 (Primary Button)

**类别**：按钮
**使用屏幕**：主菜单、暂停菜单

**描述**：最显眼的操作按钮——表示玩家最可能执行的操作。默认焦点始终落在主要按钮上。

**规格**：
- 尺寸：280×44px（暂停菜单）/ 自适应宽度（主菜单）
- 边框：3px 浓墨色（默认）、3px 松石青（聚焦）
- 填充：暖白 #f5f0eb + 5% 浓墨悬停加深
- 字体：16px 墨色，中等粗细
- 悬停：墨色加深 15%，0.15s ease-out
- 按下：墨色加深 30%，0.1s，释放后弹回
- 键盘：Enter 激活当前聚焦的按钮
- 手柄：A 键激活
- 可访问性：对比度 ≥ 4.5:1（墨色在暖白上）。焦点指示器：松石青 2px 外部光环

**何时使用**：玩家最可能需要执行的操作——新游戏、继续游戏、继续（暂停）
**何时不使用**：危险操作使用危险按钮，次要导航使用次要按钮

---

#### 次要按钮 (Secondary Button)

**类别**：按钮
**使用屏幕**：主菜单、暂停菜单、HUD（暂停入口）

**描述**：标准导航按钮——不如主要按钮突出。用于频率较低但重要的操作。

**规格**：
- 尺寸：280×44px（暂停菜单）/ 自适应（主菜单）
- 边框：1.5px 淡墨色
- 字体：16px 墨色
- 悬停：边框加粗至 2px，填充 5% 淡墨，0.15s
- 其他交互同主要按钮

**何时使用**：设置、查看卡组、保存并退出、退出
**何时不使用**：默认操作（→ 主要按钮）、破坏性操作（→ 危险按钮）

---

#### 危险按钮 (Danger Button)

**类别**：按钮
**使用屏幕**：暂停菜单

**描述**：破坏性操作——不可逆或丢失进度。通过颜色、位置和间距与安全操作明确区隔。

**规格**：
- 同次要按钮尺寸
- 边框：1.5px 朱砂红（#c03a2b）
- 字体：16px 朱砂红
- 位置：通过分隔线与安全操作区隔
- 触发：左键点击 / Enter / A 键
- 结果：打开二次确认对话框
- 可访问性：四重信号——颜色（朱砂红）+ 图标（⚠）+ 位置（底部）+ 分隔线

**何时使用**：返回主菜单（放弃进度）、战败确认
**何时不使用**：可逆或非破坏性操作

---

#### 图标按钮 (Icon Button)

**类别**：按钮
**使用屏幕**：HUD（暂停入口、战斗日志折叠）

**描述**：以图标为主的紧凑按钮——墨水线框风格，透明填充背景。

**规格**：
- 尺寸：40×40px（暂停入口）/ 24×24px（战斗日志）
- 视觉：墨色线框 2px + 透明填充
- 悬停：线框加粗至 3px + 5% 淡墨填充
- 焦点：松石青 2px 外部光环
- 手柄：方向键导航到此按钮 → A 键激活

**何时使用**：HUD 角标位置（暂停、日志、地图）
**何时不使用**：主要菜单按钮（需要文字标签 + 更大点击区域）

---

### 面板 (Panel)

#### 面板 (Panel)

**类别**：面板
**使用屏幕**：所有屏幕

**描述**：内容容器——暖白基底、墨色边框、统一圆角。所有 UI 面板遵循同一视觉语言。

**规格**：
- 基底：暖白 #f5f0eb
- 边框：1px 墨色
- 圆角：4px（小面板）/ 8px（中面板 / 暂停面板）
- 内边距：20-24px
- 阴影：无——墨骨丹青风格不使用投影，依赖边框和留白建立层级
- 动画：面板弹入/弹出遵循各屏幕的过渡规范

---

#### 覆盖层 (Overlay)

**类别**：面板
**使用屏幕**：暂停菜单、设置、卡组查看

**描述**：覆盖在当前屏幕之上的模态面板——下方的交互被阻塞。

**规格**：
- 背景：暂停菜单和过渡提示为半透明黑色遮罩
- 面板在遮罩之上居中或从边缘滑入
- 关闭方式：点击遮罩（如允许）、ESC 键、关闭按钮
- 可访问性：打开/关闭通过 AccessKit 播报（待实现）

**何时使用**：必须阻断游戏交互的模态操作
**何时不使用**：非模态信息展示（→ 通知堆叠）

---

#### 遮罩层 (Mask Layer)

**类别**：面板
**使用屏幕**：暂停菜单、过渡提示

**描述**：覆盖全屏的半透明黑色遮罩——在覆盖层背后提供视觉阻断。

**规格**：
- 颜色：#000000
- Alpha：60%（暂停）/ 按场景变化（过渡提示）
- 效果：背景模糊（暂停菜单专用——Godot 4.6 BackBufferCopy + blur shader）
- 动画：淡入 0.2-0.25s，淡出 0.2-0.3s

**何时使用**：模式层（暂停、过渡）
**何时不使用**：轻量提示或非模态信息

---

### 对话框 (Dialog)

#### 确认对话框 (Confirmation Dialog)

**类别**：对话框
**使用屏幕**：主菜单、暂停菜单

**描述**：居中弹出的小型模态面板——打断当前操作并要求明确选择。

**规格**：
- 布局：居中，约 280-320px 宽
- 标题：H3 22px + 可选图标（⚠ 用于危险确认）
- 正文：Data 16px 墨色
- 按钮：[确认操作] [取消] —— 或三按钮变体（保存并退出/不保存/取消）
- 动画：淡入 + 轻微放大 scale 0.95→1.0，0.15s
- 关闭：ESC 关闭（等效取消）、手柄 B 键关闭
- 可访问性：焦点锁定在对话框内——Tab 在按钮间循环

**何时使用**：不可逆操作（返回主菜单、保存并退出）
**何时不使用**：可逆或低风险操作

---

#### 错误提示 (Error Alert)

**类别**：对话框
**使用屏幕**：暂停菜单

**描述**：系统操作失败时的反馈对话框——保存失败、加载失败。

**规格**：
- 同确认对话框尺寸和居中布局
- 标题：⚠ 图标 + 错误标题
- 正文：具体错误描述（保存失败→磁盘/权限；加载失败→损坏/不兼容）
- 按钮：[重试] [安全回退操作]
- 不关闭底层面板——玩家不会丢失上下文

**何时使用**：数据安全关键路径的失败场景
**何时不使用**：预期或非阻塞错误（→ 通知堆叠）

---

### 输入 (Input)

#### 滑块 (Slider)

**类别**：输入
**使用屏幕**：设置（音量控制）

**描述**：水平拖动条——控制连续值参数。

**规格**：
- 轨道：2px 淡墨 #b0a89b
- 填充：轨道左侧 100% 松石青
- 滑块手柄：12px 圆形，墨色 + 暖白填充
- 交互：鼠标拖拽 / ←→ 方向键 / 手柄肩键
- 反馈：调整过程中数值实时变化（音量预览）
- 可访问性：手柄 LB/RB 以 5% 步长调整，方向键以 1% 步长调整

**何时使用**：连续值参数（音量、灵敏度）
**何时不使用**：离散选项（→ 下拉菜单）、二元状态（→ 切换开关）

---

#### 切换开关 (Toggle)

**类别**：输入
**使用屏幕**：设置（减少动态效果、全屏）

**描述**：二元状态切换——开/关。

**规格**：
- 视觉：复选框或滑动切换——以美术圣经 §7 最终视觉为准
- 交互：左键点击 / Enter / 空格 / 手柄 A 键
- 反馈：开关状态即时视觉变化
- 状态：开（松石青高亮）/ 关（淡墨默认）
- 可访问性：标签声明当前状态（"减少动态：开"）

**何时使用**：二元设置项
**何时不使用**：多选项之间选择（→ 下拉菜单）

---

#### 下拉菜单 (Dropdown)

**类别**：输入
**使用屏幕**：设置（分辨率、帧率限制、画面质量、语言）

**描述**：点击展开选项列表——选择一个后列表收起。

**规格**：
- 触发器：点击显示当前值 → 选项列表向下展开
- 展开列表：暖白基底 + 1px 墨色边框 + 4px 圆角
- 选中项：松石青背景 + 墨色文字高亮标记
- 导航：↑↓ 方向键浏览选项，Enter/A 键确认
- 关闭：点击外部 / ESC / 选择后自动关闭
- 动画：列表从触发器向下滑出 0.15s

**何时使用**：3-10 个互斥选项的选择
**何时不使用**：2 个选项（→ 切换开关）、连续值（→ 滑块）

---

### 数据显示 (Data Display)

#### 进度条 (Progress Bar)

**类别**：数据显示
**使用屏幕**：HUD（修为、行动力、HP）

**描述**：线性填充条——可视化比例值（当前/上限）。

**规格**：
- 轨道：2px 淡墨线框
- 填充颜色：阈值切换——<50% 蓝 → 50-90% 紫 → ≥90% 金（修为）/ 松石青（行动力）/ 朱砂红（HP <20%）
- 尺寸：240×60px（境界修为）/ 72px 宽（角色 HP）/ 180px 宽（行动力）
- 更新事件驱动——非轮询
- 动画：平滑填充 0.3-0.4s ease-out
- 阈值闪烁：HP <20% = 0.5s 周期，修为 ≥90% = 1.0s 呼吸周期
- 可访问性：数字标签始终显示当前/上限值——不依赖颜色。色盲玩家通过长度和数字读取

**何时使用**：需要展示比例数据的场景
**何时不使用**：精确数值（→ 状态图标+计数器）、离散计数（→ 计数器）

---

#### 状态图标 + 计数器 (Status Icon + Counter)

**类别**：数据显示
**使用屏幕**：HUD（灵石、卡组、牌库、弃牌堆）

**描述**：小型图标配数字——用于资源数量、计数器和状态指示。

**规格**：
- 图标：24-28px 象形图标（灵石）/ 20px（牌库计数）
- 数字：Data 层级 20px 等宽数字，墨色常态
- 布局：图标 + 数字水平排列 HUD 资源栏
- 更新事件驱动——数值变更时触发
- 动画：数值跳动 scale 1.0→1.3→1.0，400ms
- 格式化：≥1000 显示 "1.2k"
- 阈值：卡组 = 上限时黄色，> 上限时红色闪烁 0.3s

**何时使用**：需要精确数值 + 快速扫读的场景
**何时不使用**：不需要精确数值（→ 进度条）、非数值状态（→ 状态图标）

---

#### 静态文本 (Static Text)

**类别**：数据显示
**使用屏幕**：所有屏幕（标题、版本号、进度信息）

**描述**：只读文本——不可交互。

**规格**：
- 字号层级遵循美术圣经 §7.2：H1 36px / H2 28px / H3 22px / H4 18px / Data 16px / Small 13px / Micro 10px
- 颜色：浓墨 #1a1a1a（主要内容）/ 淡墨 #8c8580（辅助信息）
- 对比度：≥ 4.5:1（浓墨在暖白上）
- 本地化：所有静态文本为字符串键——不硬编码

**何时使用**：任何不需要交互的文本元素
**何时不使用**：需要交互的文本（→ 按钮）

---

#### 悬停提示 (Tooltip)

**类别**：数据显示
**使用屏幕**：HUD（修为详情）、探索 UI（地图节点悬停）

**描述**：鼠标悬停在元素上时出现的浮动信息面板——显示该元素无法在主视图中容纳的额外数据。

**规格**：
- 外观：暖白基底 + 1px 墨色边框 + 4px 圆角
- 定位：跟随鼠标，偏移 12px——不遮挡被悬停的元素
- 触发：鼠标悬停 300ms 延迟显示
- 关闭：鼠标移开立即消失（无延迟）
- 字号：Small 13px 墨色
- 可访问性：键盘聚焦时等效显示——无鼠标专用

**何时使用**：辅助详情（精确数值、卡牌详情）
**何时不使用**：关键信息（→ 内联显示）、操作指令（→ 按钮标签）

---

### 通知 (Notification)

#### 通知堆叠 (Notification Stack)

**类别**：通知
**使用屏幕**：HUD

**描述**：可堆叠、自动消失的非模态通知——出现在中央顶部，不打断玩家操作。

**规格**：
- 位置：中央顶部，center-x，top: 72px
- 尺寸：每条 360×32px
- 堆叠上限：3 条
- 队列策略：FIFO——超量时丢弃最早的普通通知。系统通知和错误通知优先保留
- 持续时间：按类型——道具/卡牌/灵石/修为 2-3s、战斗事件 3s、系统/错误 4s
- 颜色编码：松石青（获得/正向）、琉璃金（稀有/灵石）、朱砂红（危险/错误）、淡墨（中性/系统）
- 交互：点击通知立即关闭
- 动画：出现时从顶部滑入 0.2s，消失时滑出+淡出 0.2s
- 可访问性：Screen reader 播报通知类型和内容（待实现）

**何时使用**：非阻塞的短暂反馈
**何时不使用**：阻塞性反馈（→ 对话框）、需要玩家确认的操作（→ 确认对话框）

---

### 导航 (Navigation)

#### 标签页导航 (Tab Navigation)

**类别**：导航
**使用屏幕**：主菜单、暂停菜单、设置

**描述**：键盘 Tab 键在可聚焦元素间的顺序移动。焦点顺序遵循视觉布局——自上而下、自左而右。

**规格**：
- 主菜单：标题（不可聚焦）→ 新游戏 → 继续 → 设置 → 退出 → 循环
- 暂停菜单：继续 → 查看卡组 → 系统设置 → 保存并退出 → 返回主菜单 → 循环（战斗中跳过保存并退出）
- Shift+Tab：反向遍历
- Enter / Space：激活聚焦元素
- ESC：关闭面板/对话框

**何时使用**：所有屏幕始终启用——不可选择退出
**何时不使用**：N/A——这是始终活跃的基础行为

---

#### 焦点环 (Focus Ring)

**类别**：导航
**使用屏幕**：所有屏幕

**描述**：可视化键盘/手柄焦点的指示器——松石青 2px 外部光环，标记当前聚焦的元素。

**规格**：
- 外观：松石青 #2d8f7f 2px 外部光环，圆角匹配元素圆角
- 键盘：Tab/Shift+Tab 移动焦点
- 手柄：方向键/左摇杆移动焦点
- 缺失时：键盘/手柄导航下不可见——焦点可能丢失
- 实现约束：Godot 4.6 双焦点系统——Control.focus_mode 必须显式地为所有交互式元素设罔为 FocusMode.ALL

**何时使用**：所有可聚焦元素——绝不省略
**何时不使用**：N/A

---

### 布局 (Layout)

#### 分隔线 (Divider)

**类别**：布局
**使用屏幕**：暂停菜单

**描述**：1px 水平线——将按钮组分隔为安全区和危险区。

**规格**：
- 视觉：1px 淡墨 #b0a89b
- 宽度：320px（暂停面板全宽）
- 间距：分隔线上方和下方各留空
- 目的：视觉分组——安全操作在上，危险操作在下

**何时使用**：当按钮组有不同的安全级别时（保存 vs 丢弃）
**何时不使用**：同质按钮组（→ 统一间距即可）

---

#### 按钮组 (Button Group)

**类别**：布局
**使用屏幕**：暂停菜单

**描述**：等宽竖排按钮——统一间距，排序反映信息层级。

**规格**：
- 布局：竖排，间距 8px
- 宽度：统一（280px 暂停菜单按钮）
- 排序：按使用频率——最常见在上，危险在底部
- 分隔：危险操作通过分隔线与上方组分隔

**何时使用**：2 个以上等宽竖排按钮
**何时不使用**：单按钮、混合宽度按钮、横排布局

---

### 动画 (Animation)

#### 过渡覆盖层 (Transition Overlay)

**类别**：动画
**使用屏幕**：HUD（场景切换）

**描述**：场景切换时的全屏覆盖层——向玩家传达过渡状态。

**规格**：
- 视觉：半透明黑色遮罩 #000000 60% alpha + 居中 H2 28px 文字 + 场景图标
- 内容按场景配对：探索→战斗 = "进入战斗" + � + 战斗名 1.0s
- 加载态：+ 旋转太极图加载指示器
- 动画：遮罩淡入 0.2s → 文字+图标缩放弹入 0.3s → 驻留 → 遮罩淡出 0.3s
- 无玩家交互——完全动画驱动

**何时使用**：场�切换
**何时不使用**：面板级过渡（→ 覆盖层）、加载指示器（→ 本模式在持续 > 1s 时隐式包含加载态）

---

### 游戏专用 (Game-Specific)

#### 手牌布局 (Card Hand Layout)

**类别**：游戏专用
**使用屏幕**：战斗 HUD

**描述**：弧形排列玩家手牌——焦点卡放大，其余保持标准尺寸。

**规格**：
- 排列：弧形（曲率半径由牌数决定——7 张 vs 10 张）
- 牌尺寸：100×140px（手牌中），宽高比 5:7
- 悬停态：scale 1.2× + 上浮 10px + 完整牌面详情浮现——其余牌不位移
- 拖拽态：scale 1.1 + 下方 8px 淡墨阴影——跟随鼠杯
- 不可用态：40% 透明度 + 费用图标灰色——不可拖拽
- 溢出：>7 张堆叠，>10 张启用左右滚轮滚动
- 输入：鼠标拖拽 / 数字键 1-7 选择 / 手柄方向键选择 + A 确认打出
- 动画：抽牌飞入 300ms、打出飞向目标 400ms、重新排列 200ms ease-out

**何时使用**：战斗——出牌阶段
**何时不使用**：探索/商店/事件（→ HUD 资源栏）

---

#### 回合阶段指示器 (Turn Phase Indicator)

**类别**：游戏专用
**使用屏幕**：战斗 HUD

**描述**：战斗顶部居中水平进度条——显示当前阶段名 + 7 格进度跟踪。

**规格**：
- 视觉：H3 22px 浓墨当前阶段名 + 下方 4px 淡墨线框 ×7 格
- 7 阶段：备战 → 抽牌 → 行动 → 攻击 → 结束 → 结算 → 回合结束
- 更新：事件驱动——phase_changed 信号
- 动画：阶段切换文字 slide-in 200ms，进度格逐一填充 150ms
- 无玩家交互——只读显示

**何时使用**：战斗（始终在战斗中显示）
**何时不使用**：探索/商店/事件/地图选择

---

#### 角色状态卡 (Character Status Card)

**类别**：游戏专用
**使用屏幕**：战斗 HUD（敌方角色区、己方角色区）

**描述**：角色头像+状态信息的复合显示卡——头像占满卡牌背景，功法/法宝/HP/ATK/buff/境界等元素分层叠加在头像上层。支持前排（100% 缩放实线边框）/后排（85% 缩放虚线边框）区分。

**规格**：
- 尺寸：前排 120×168px 100% 缩放实线边框；后排 85% 缩放虚线边框
- 内部分层（从底到顶）：
  - L0 底层：角色头像占满卡牌（全幅背景）
  - L1 遮罩：底部渐变黑色遮罩（40-60%，保证上层文字可读）
  - L2 左侧栏（28px 宽）：功法图标竖排（最多 3 个，每个 20×20px，间距 4px）
  - L2 右侧栏（28px 宽）：法宝图标竖排（最多 3 个，每个 20×20px，间距 4px）
  - L3 底部条（40px 高）：HP 条 + ATK 数字
  - L4 左下角：buff/debuff 图标区（最多 3-5 个）
  - L4 右下角：境界图标（不同境界用不同图标，悬停显示境界名文字）
  - L5 左上角：待命/已行动标记（仅己方）
- 悬停：微发光+悬浮详情面板展开（互斥——同时最多一个）
- 阶段 3：可被点击/拖拽选为攻击目标（红色高亮+锁定标记）
- 无障碍：境界用图标非仅颜色；待命/已行动用图标+文字标签

**何时使用**：战斗场景显示任何已上场角色（敌我通用）
**何时不使用**：手牌中的卡牌（→ 手牌布局）、备战选择列表（→ 备战阵位预览）

---

#### 阵法槽位 (Formation Slot)

**类别**：游戏专用
**使用屏幕**：战斗 HUD（顶部条阵法区）

**描述**：已部署阵法卡的展示槽位——激活阵法显示图标+效果简述，空位显示虚线框。

**规格**：
- 位置：顶部条右侧
- 已激活态：阵法图标+效果简述，悬停微发光+详情浮窗展开（效果全文）
- 空位态：虚线框占位
- 可交互：悬停查看效果（只读——阵法部署在备战阶段完成）
- 键盘/手柄：Tab/方向键可达，焦点环显示

**何时使用**：战斗中展示当前激活阵法
**何时不使用**：备战阶段的阵法选择（→ 备战阵位预览）

---

#### 备战阵位预览 (Deployment Preview Grid)

**类别**：游戏专用
**使用屏幕**：战斗（阶段 0 备战面板）

**描述**：全屏覆盖层——角色选择列表+前后排阵位布局预览，支持拖拽调整阵位。

**规格**：
- 结构：左侧角色选择列表 + 右侧阵位布局预览（前排/后排格位）
- 选择角色：点击/数字键/手柄方向键+A——角色加入阵位
- 取消选择：再次点击已选角色/再按数字键/B 键
- 拖拽调整：左键按住拖拽角色到阵位（阵位框高亮+角色跟随鼠标）
- 替换已满：点击⊕标记角色 → 弹出替换确认弹窗
- 确认/返回：「确认」Enter/Start 进入阶段 1；「返回」ESC/B 退出战斗
- 无障碍：所有操作有键盘与手柄等价路径

**何时使用**：阶段 0 备战（角色上场与阵位调整）
**何时不使用**：战斗中调整（阶段 0 外阵位锁定）

---

#### 攻击目标选择 (Attack Target Selection)

**类别**：游戏专用
**使用屏幕**：战斗（阶段 3 攻击声明）

**描述**：全屏叠加的目标选择层——蓝色脉冲标记可攻击角色、红色高亮可选目标、锁定标记不可选目标、箭头连线显示攻击关系。

**规格**：
- 点击选目标：左键点击红色高亮目标 / Tab+Enter / 手柄方向键+A → 目标锁定标记+箭头连线+确认音
- 拖拽选目标：从攻击者拖拽到目标——箭头连线实时跟随鼠标，释放在目标上→声明攻击
- 取消：点击其他目标 / 右键 / ESC / B 键
- 状态提示：「点击/拖拽选择攻击目标」文字提示，动画后淡出
- 无手牌/无可攻击目标时不可进入此层

**何时使用**：阶段 3 攻击声明
**何时不使用**：其他阶段（层隐藏）、法术类无需目标的卡牌（直接出牌）

---

#### 战利品选择网格 (Loot Selection Grid)

**类别**：游戏专用
**使用屏幕**：战斗结算（胜利）

**描述**：三选一奖励选择——卡牌/灵石/消耗品选项以卡片网格呈现，悬停查看详情，选中后二次确认。

**规格**：
- 布局：3 个选项卡片水平排列（网格）
- 悬停：选项 scale 1.05× + 外发光，显示奖励详情
- 选中：左键点击 / 数字键 1-3 / 手柄方向键+A → 选中高亮+二次确认弹窗
- 确认：「确认」Enter/A → 奖励飞向 HUD 动画+面板关闭（触发 `loot_selected` 持久事件）
- 跳过：「跳过」ESC/B → 「放弃所有奖励？」确认弹窗
- 无障碍：选项详情不依赖悬停（键盘聚焦同样展开详情）

**何时使用**：战斗胜利结算
**何时不使用**：事件奖励（→ 事件选项面板）、通关奖励（→ 结算面板）

---

#### 节点图节点 (Node Graph Node)

**类别**：游戏专用
**使用屏幕**：探索 UI（节点图主界面）

**描述**：探索节点图上的可点击节点——10 种类型（入口/战斗/精英/Boss/事件/商店/传送/灵泉/渡劫台/回复点）用图标+颜色双编码，5 种状态（可达/不可达/已访问/迷雾/当前位置）。

**规格**：
- 类型编码：图标+颜色双编码（色盲友好）；悬停显示类型名
- 状态视觉：可达=高亮+呼吸描边；不可达=灰显+禁用光标；已访问=暗淡+勾标记；迷雾=「？」遮罩；当前位置=玩家标记
- 悬停：节点放大 110% + tooltip 浮出（类型名、敌人构成[战斗类]、AP 消耗、风险提示）；迷雾节点仅显示「？」
- 点击可达节点：移动（系统自动路由路径），扣 AP，揭雾动画
- 点击不可达节点：抖动+toast「行动力不足」/「路径不可达」
- 点击判定半径 ≥24px（手柄虚拟光标磁性吸附）
- 键盘：方向键按空间关系跳转（↑下层/↓上层/←→同层），Enter 确认
- Boss 邻近：呼吸发光动画（2s 周期）

**何时使用**：探索节点图
**何时不使用**：地图选择界面（→ 地图选择卡片）

---

#### 路径连线 (Path Connection)

**类别**：游戏专用
**使用屏幕**：探索 UI（节点图主界面）

**描述**：节点图上节点间的连线——传达可达性与迷雾状态，纯视觉指示不可点击。

**规格**：
- 可达路径：高亮色描边
- 不可达路径：暗淡色
- 迷雾段：虚线
- 悬停节点时：该节点可达路径高亮加强
- 玩家移动动画：玩家标记沿路径移动（~0.4s/节点），途经节点依次点亮
- 无障碍：可达性同时由节点状态视觉传达（连线仅为辅助），不单靠连线亮度区分

**何时使用**：探索节点图（始终显示）
**何时不使用**：地图选择界面

---

#### 迷雾遮罩 (Fog of War Overlay)

**类别**：游戏专用
**使用屏幕**：探索 UI（节点图主界面）

**描述**：覆盖未探索区域的半透明迷雾——传达「未知」感，随玩家移动逐步揭开。

**规格**：
- 默认态：半透明迷雾覆盖未探索区域，迷雾节点显示「？」
- 揭雾动画：移动到新节点后相邻迷雾消散（~0.8s），揭示节点弹入（scale 0.8→1.0，ease-out）
- 减少动态：瞬时揭示+一次性边缘高亮
- 不可交互——纯视觉层
- 性能注意：迷雾区域需合批渲染（Draw Call 预算内）

**何时使用**：探索节点图（未探索区域）
**何时不使用**：已探索区域、地图选择界面

---

#### 画布缩放平移 (Canvas Zoom & Pan)

**类别**：游戏专用
**使用屏幕**：探索 UI（节点图主界面）

**描述**：节点图画布的视图控制——滚轮缩放 50%-150% + 平移，默认整图自动适配屏幕。

**规格**：
- 缩放：鼠标滚轮 / 手柄 LB/RB，范围 50%-150%，平滑过渡（~0.2s）
- 平移：中键拖拽 / 边缘拖拽 / 缩放激活时手柄方向键，光标变抓手
- 复位：空格键 / 手柄 L3——回到整图自动适配
- 缩放联动：50% 时隐藏节点文字标签只留图标（避免低于可读阈值的文字）
- 弹窗打开时冻结（缩放/平移/点击均无效）
- 默认视图：整图自动适配——保证「一眼看全貌」零操作成本

**何时使用**：探索节点图（节点数量多需细察时）
**何时不使用**：战斗、菜单界面

---

#### 结算面板 (Summary Panel)

**类别**：游戏专用
**使用屏幕**：探索（探索结束、地图通关）、战斗（战败）

**描述**：全屏结算展示——收集摘要/奖励列表/Boss 状态等结果的分段呈现，含确认返回操作。

**规格**：
- 布局：全屏覆盖，标题+分段内容列表+确认按钮
- 探索结束变体：收集摘要（灵石/卡牌/修为）+ Boss 状态行（未遭遇——到达第 X/Y 层 / 已遭遇——结果）+「返回地图选择」
- 通关结算变体：通关奖励（常规+首次通关额外奖励分段，重入不显示后者）+ 新地图解锁+「确认」
- 战败变体：损失摘要+确认返回（此前已在模式库缺口中标记，本模式统一覆盖）
- 空态：摘要为空显示「本次探索无收获」占位文案
- 进入动画：淡入+轻微下沉感（~0.4s）；通关变体金色光晕展开（~1s）
- 确认操作触发持久事件（`map_cleared` 等）——需二次确认已在入口弹窗完成

**何时使用**：任何探索/战斗的阶段性结算
**何时不使用**：战斗胜利三选一（→ 战利品选择网格）、事件结果（→ 事件选项面板）

---

#### 地图选择卡片 (Map Selection Card)

**类别**：游戏专用
**使用屏幕**：地图选择界面

**描述**：地图选择界面的选择卡片——名称/插图/难度/境界要求/通关状态/解锁状态，三种状态（未通关可进/已通关重入/锁定）。

**规格**：
- 尺寸：~360×420px，横向滚动列表排列
- 未通关+境界达标：正常态，悬停上浮+边框高亮+详情 tooltip，点击直接进入（0 灵石）
- 已通关：显示 ✓ 标记，点击弹出重入确认弹窗（扣费金额+当前灵石+确认/取消）
- 锁定（境界不足/未解锁）：灰显+锁图标双编码，悬停 tooltip 显示解锁条件
- 键盘/手柄：方向键在卡片间移动，A/Enter 进入
- 标题单行约束：按 8 个中文字符宽度预留（本地化 +40% 膨胀），超出截断+tooltip 全名

**何时使用**：地图选择界面
**何时不使用**：节点图内（→ 节点图节点）

---

#### 商品卡 (Shop Item Card)

**类别**：游戏专用
**使用屏幕**：坊市（买卡标签页）

**描述**：坊市商品网格中的购买卡位——卡牌缩略图+卡名+稀有度色条+类型标签+价格，四种状态（可购买/灵石不足/卡组已满/已售）。

**规格**：
- 网格 4 列默认（3~6 列调优）；卡面信息完整自持（无需上下文）
- 可购买：正常态，悬停放大 1.05×+外发光+卡牌详情浮窗（0.3s 内）；点击弹购买确认
- 灰态（灵石不足/卡组已满）：变灰 + 文字标记双编码（非仅颜色）；点击抖动+toast
- 已售：❌标记+不可交互
- 键盘/手柄：方向键网格导航（行优先），A/Enter=购买确认
- 价格区按 5 位灵石数字+千分位预留；卡名 12 字内单行

**何时使用**：坊市购买商品展示
**何时不使用**：战利品选项（→ 战利品选择网格）；卡组浏览（→ 网格卡位，无价格）

---

#### 散功选择网格 (Deck Removal Grid)

**类别**：游戏专用
**使用屏幕**：坊市（散功/售卡标签页）

**描述**：当前卡组的网格选择器——散功页为单选约束（每次删 1 张），售卡页为多选变体（总价累计）。与超限弃牌网格同族但约束不同。

**规格**：
- 当前卡组全部卡牌（按类型分组可选），滚动浏览
- 单选模式（散功）：点选一张 ✓，再点同卡取消、点他卡换选；底部费用+确认按钮随选择激活
- 多选模式（售卡）：多选 ✓，底部「已选 N 张 = X 灵石」实时累计
- 底部操作条显示当前费用/总价（数值系统返回，UI 不计算递增）
- 卡组 ≤5 张保护：确认禁用+「卡组至少保留5张」
- 确认均走二次确认弹窗（「删除后不可恢复」/「出售后不可恢复」），焦点默认取消侧
- 键盘/手柄：方向键+Enter/A 选择，网格吸附导航

**何时使用**：坊市散功（单选）/售卡（多选）
**何时不使用**：超限弃牌（→ 超限弃牌网格——数量上限+无取消出口）；只读卡组浏览（→ 无选择态）

---

#### 超限弃牌网格 (Overlimit Discard Grid)

**类别**：游戏专用
**使用屏幕**：超限弃牌弹窗（事件/战利品触发）

**描述**：强制模态中的卡组多选网格——数量上限约束（需弃 N 张）+ 确认门（选满激活）+ 无取消出口（唯一退出路径是确认）。

**规格**：
- 红色警示样式头部：卡组 X/上限（超限 N 张）+ 进度「已选 X（还需 Y）」+ 补偿预览（+Z 灵石，每张 5 灵石）
- 多选上限 N：选满后再点提示「已达弃牌数上限」，需先取消一张
- 确认按钮仅选满激活：「确认弃牌 ✓（获得 Z 灵石）」
- **无取消路径**——强制完成取舍（GDD 设计理念：不是惩罚而是知情取舍）
- 进入前必经「你获得了以下卡牌」知情弹窗（不可跳过）
- 键盘/手柄：方向键+Enter/A 选择，焦点落于网格首卡

**何时使用**：事件导致卡组超境界上限时
**何时不使用**：坊市散功（→ 散功选择网格——可选取消、灵石付费、单选）

---

## 缺口与待补充模式 (Gaps & Patterns Needed)

以下模式在现有 UX 规范中被引用但尚未正式定义——将在对应屏幕的 UX 设计时补充：

| 模式 | 需要它的屏幕 | 优先级 |
|------|------|:--:|
| 卡牌详情面板 (Card Detail Panel) | 卡组编辑、战斗 HUD（悬停完整牌面）、探索（卡组查看详情） | 高 |
| 身份选择轮盘 (Identity Selection Carousel) | 开局身份选择 | 高 |
| 滚动日志面板 (Scrollable Log Panel) | 战斗 HUD（日志展开态） | 中 |
| 制作人员滚动列表 (Credits Scrol) | 通关片尾 | 低 |
| 按键重绑定界面 (Key Rebinding UI) | 设置 | 低 |
| 存档槽选择界面 (Save Slot Selector) | 主菜单（继续游戏多存档） | 低 |
| 卡组编辑网格 (Deck Editing Grid) | 卡组编辑 UI | 高 |
| 商店物品列表 (Shop Item List) | 商店界面 | 中 |
| 事件选项面板 (Event Choice Panel) | 事件界面 | 中 |

**2026-09-07 更新**：以下模式已从缺口列表移除并正式定义（combat-ui 与 exploration-ui UX 规范引入）：
- ~~地图节点图 (Map Node Graph)~~ → 拆分为节点图节点 + 路径连线 + 迷雾遮罩 + 画布缩放平移（4 个模式）
- ~~战敗结算面板 (Defeat Summary Panel)~~ → 并入结算面板（Summary Panel，统一覆盖探索结束/通关/战败变体）
- ~~通关结算面板 (Victory Summary Panel)~~ → 并入结算面板
- 新增：角色状态卡、阵法槽位、备战阵位预览、攻击目标选择、战利品选择网格、地图选择卡片（combat-ui 5 个 + exploration-ui 6 个 = 11 个，总计 12 个游戏专用模式）

---

## 模式库维护

- 新屏幕的 `/ux-design` 发现新模式 → 追加到本库
- `/ux-review` 检查模式一致性 → 标记偏离
- 模式名称不得重复——使用描述式名称
- 每个模式**一个归属类别**——交叉引用通过模式名称实现
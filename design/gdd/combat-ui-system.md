# 战斗UI系统 (Combat UI System)

> **状态 (Status)**：设计中 (In Design)
> **作者 (Author)**：Claude Code + 用户
> **最后更新 (Last Updated)**：2026-07-22
> **最后验证 (Last Verified)**：—
> **实现的支柱 (Implements Pillar)**：支柱1「自由组牌，策略为王」

## 概述

战斗UI系统负责战斗场景中所有面向玩家的视觉交互界面——包括手牌区、场上角色区、费用栏、阵法区、敌方信息区、回合阶段指示器、攻击目标选择以及战斗结算面板。战斗UI系统不执行战斗逻辑（那是战斗系统的职责），而是将战斗系统的状态数据转化为玩家可理解、可操作的视觉界面，并将玩家的操作（出牌、选目标、布阵）转化为战斗系统的指令。

对玩家来说，战斗UI就是他们与游戏核心交互的直接界面。每一局战斗的所有操作——从出牌到攻击、从布阵到撤退——都通过战斗UI完成。UI的响应速度、信息清晰度和操作流畅度直接决定了玩家对「策略为王」支柱的感知质量。

## 玩家幻想

战斗UI的核心情感是**清晰的掌控感**——玩家在战斗中每时每刻都能清楚地知道：我有多少费用、我的手牌有什么、我的角色状态如何、敌人下一步可能做什么。UI不应让玩家「找信息」，而应让信息主动呈现给玩家。

| 情感 | 触发场景 |
|------|---------|
| **掌控的清晰** | 一眼看到费用、手牌、场上状态——「我知道现在是什么局面」 |
| **操作的流畅** | 拖拽出牌到目标的无缝体验——「指哪打哪」 |
| **反馈的及时** | 打出卡牌后的即时效果展示——「我知道这张牌生效了」 |
| **战局的透明** | 看到敌方状态和buff/debuff——「我知道敌人在干什么」 |
| **结算的释然** | 战斗胜利时奖励逐项展示——「这仗打得值」 |

战斗UI的核心原则是：**不干扰决策，只辅助决策**。UI应让玩家的注意力集中在策略思考上，而非解读界面元素上。

## 详细设计

### 核心规则

#### 1. 战斗HUD整体布局

```
┌──────────────────────────────────────────────────────┐
│ [回合阶段]                    [阵法区]    [撤退]     │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 敌方前1   │  │ 敌方前2   │  │ 敌方前3   │ ←敌方侧  │
│  └──────────┘  └──────────┘  └──────────┘          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 敌方后1   │  │ 敌方后2   │  │ 敌方后3   │          │
│  └──────────┘  └──────────┘  └──────────┘          │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 己方前1   │  │ 己方前2   │  │ 己方前3   │ ←己方侧  │
│  └──────────┘  └──────────┘  └──────────┘          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 己方后1   │  │ 己方后2   │  │ 己方后3   │          │
│  └──────────┘  └──────────┘  └──────────┘          │
│                                                      │
│  ┌──────────────────────────────┐                    │
│  │ [费用栏] 5/5                 │                    │
│  ├──────────────────────────────┤                    │
│  │ [手牌区] 弧形排列7张卡牌      │ ←底部              │
│  └──────────────────────────────┘                    │
└──────────────────────────────────────────────────────┘
```

屏幕从上到下依次为：回合阶段信息区 + 阵法区 → 敌方角色区 → 己方角色区 → 费用栏 → 手牌区

#### 2. 手牌区

- 位于屏幕底部，以**弧形**排列当前手牌
- 手牌区域高度约屏幕的25%，宽度100%
- 每张卡牌以**缩小版正面**展示（卡名、费用图标（左上角）、稀有度颜色边框、类型标签）
- 手牌超过7张时堆叠排列（部分重叠），玩家可左右滑动/滚轮浏览
- 费用不足的卡牌**灰显**（降低饱和度+费用图标变灰），不可拖拽
- 可拖动的卡牌在鼠标悬停时放大120%，显示卡牌详情（效果全文）
- 拖拽出牌：鼠标按住卡牌向目标区域拖拽释放，拖拽过程中卡牌跟随鼠标

**手牌区状态：**

| 状态 | 视觉表现 | 说明 |
|------|---------|------|
| **默认** | 弧形排列，卡牌彩色，费用正常显示 | 所有手牌均可查看 |
| **费用不足** | 卡牌灰显，费用数字红色 | 费用<卡牌费用，不可出牌 |
| **目标选择中** | 可用的卡牌高亮（金色边框），不可用的深灰 | 已选中一张卡牌，正在选目标 |
| **放大预览** | 鼠标悬停的卡牌放大+详细信息弹出 | — |
| **拖拽中** | 卡牌跟随鼠标，半透明效果 | — |
| **阶段2之外** | 卡牌完全灰显，不可交互 | 出牌阶段外的抽牌/攻击/敌方阶段 |

#### 3. 场上角色区

己方和敌方角色各占屏幕的一部分：

**角色位展示信息：**
```
┌────────────────────────┐
│ [角色头像]  [境界标记]  │
│ [角色名称]  [HP条]     │ ← HP条显示当前/最大HP
│ [状态图标区]            │ ← buff/debuff图标排列
│ [绑定功法图标] [绑定法宝] │ ← 已绑定的卡牌小图标
└────────────────────────┘
```

- 角色头像：圆形裁切，背景色代表阵营/稀有度
- HP条：绿色→黄色→红色（HP<30%时），数字显示当前/最大HP
- 境界标记：仅高于玩家境界时显示↑标记
- 状态图标区：buff 图标在左（绿色边框），debuff 图标在右（红色边框）
- 绑定功法/法宝：显示绑定卡牌的小型图标（空位显示灰色槽位）
- 待命角色：角色头像外圈显示沙漏标记，HP条上方显示「待命」文字
- 已行动角色：角色头像外圈显示灰色遮罩，「已行动」标记
- 阵亡角色：角色位显示骷髅+消散动画，移除后变为空位（虚线框）

**前后排视觉区分：**
- 前排角色位边框为实线（金色/黄色），后排为虚线（蓝色）
- 前后排之间有明显间距分隔线
- 空位以浅色虚线框表示

#### 4. 费用栏

位于手牌区上方、角色区下方：

```
┌─────────────────────────────────────┐
│  ⚡ 5 / 5     [+1临时]   ≡牌库:12  │
└─────────────────────────────────────┘
```

- 费用图标（闪电/灵力标志）+ 当前费用 / 上限
- 当前费用数字颜色：≥上限50%时白色，<50%时黄色，0时红色
- 临时费用（丹药增加）显示为额外绿色+数字
- 右侧显示牌库剩余张数（点击可展开牌库详情）
- 弃牌堆显示在旁边（灰色+数字），点击可展开弃牌堆详情

#### 5. 阵法区

位于屏幕顶部中间：

```
┌──────┐ ┌──────┐ ┌──────┐
│阵·天  │ │阵·魔  │ │ 空   │  ← 最多3个
│南盟阵 │ │域血阵 │ │      │
└──────┘ └──────┘ └──────┘
```

- 已部署阵法以卡片形式展示（阵法名+阵营标签）
- 空阵法位以灰色虚线框显示
- 阵法激活效果（光环）以阵法卡片外发光表示（颜色对应阵营）
- 鼠标悬停阵法卡→显示阵法效果详情

#### 6. 回合阶段指示器

位于屏幕左上角：

```
阶段 2/7 · 出牌阶段
████████░░░░░░░ 60%
```

- 显示当前阶段名称（准备/抽牌/出牌/布阵/攻击声明/攻击结算/敌方行动/结束）
- 阶段进度条（当前阶段剩余时间或简单进度指示）
- 回合编号显示（第X回合）
- 阶段切换时：阶段名短暂放大动画+轻微音效

#### 7. 攻击目标选择

阶段4（攻击声明）的交互流程：

```
① 当前可攻击的己方角色头像外圈显示蓝色脉冲光效
② 玩家点击一个己方角色（或已预设了攻击目标）
③ 该角色可攻击的目标（敌方角色）高亮：红色边框+脉冲
④ 不可攻击的目标（前排保护下的后排）灰色 + 🔒标记
⑤ 点击一个敌方目标 → 确认攻击声明
⑥ 所有己方角色都声明完攻击目标后 → 自动进入阶段5
⑦ 玩家也可跳过某个角色的攻击（角色上显示「跳过」按钮）
```

- 一回合内多个角色的攻击声明可任意顺序进行
- 已经声明目标的角色显示目标箭头指向
- 玩家可在阶段4结束前修改已声明的目标（点击角色→清除已有目标→重新选择）
- 阶段5（攻击结算）自动播放，不可交互

#### 8. 备战阶段UI

战斗开始前的界面：

```
┌──────────────────────────────────────┐
│  ⚔ 七玄门·山脚 — 普通战斗            │
│                                      │
│  角色选择（上限4人）                    │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ │
│  │角色1│ │角色2│ │角色3│ │角色4│ │角色5│ │
│  │  ✓  │ │  ✓  │ │  ✓  │ │  ☐  │ │  ☐  │ │
│  │HP:8 │ │HP:6 │ │HP:10│ │HP:7 │ │HP:9 │ │
│  │ATK:3│ │ATK:4│ │ATK:2│ │ATK:3│ │ATK:4│ │
│  └────┘ └────┘ └────┘ └────┘ └────┘ └────┘ │
│  ┌────┐ ┌────┐                              │
│  │角色6│ │角色7│ ← 不可用（灰色+骷髅）       │
│  │  ☐  │ │  ☐  │                              │
│  └────┘ └────┘                              │
│                                      │
│  阵位布局预览：                    │
│  [前排] 角色1  角色2  角色3    │
│  [后排] 角色4  (空)    (空)    │
│                                      │
│  [确认出战]  [返回探索]              │
└──────────────────────────────────────┘
```

**备战界面规则：**
- 角色卡以缩略卡片形式展示（角色头像、HP、ATK、绑定概览）
- 已选角色打✓标记，绑定卡图标缩略显示在角色卡右下角
- 不可用角色（灰色+骷髅标记）不可选择，鼠标悬停提示阵亡状态
- 已选人数不满上限时显示提示「还可以选择X人」
- 阵位布局预览实时更新（拖拽角色到头像区自动排位）
- 确认按钮在选择了至少1个可用角色后可点击

#### 9. 战斗结算面板

**胜利面板：**
```
┌──────────────────────────┐
│         🎉 胜利！          │
│                           │
│  战利品选择（3选1）：      │
│  ┌────────┐ ┌────────┐   │
│  │  青元剑诀 │ │  灵石   │   │
│  │  (蓝·功法) │ │  ×25   │   │
│  └────────┘ └────────┘   │
│  ┌────────┐              │
│  │  回血丹  │              │
│  │  (消耗品)│              │
│  └────────┘              │
│                           │
│  [确认选择]                │
└──────────────────────────┘
```

**失败面板：**
```
┌──────────────────────────┐
│         💀 战败            │
│                           │
│  损失50%本次战斗修为       │
│  阵亡角色：韩立（不可用）   │
│                           │
│  [返回探索]                │
└──────────────────────────┘
```

- 胜利时弹出三选一战利品面板（标准卡牌正面展示 / 灵石图标+数值 / 消耗品）
- 战败时弹出损失摘要（修为损失、阵亡角色列表、绑定卡失去提示）
- 结算面板在展示完成后需玩家点击确认才关闭（不自动关闭）

#### 10. 撤退确认

```
┌──────────────────────────┐
│  ⚠ 确认撤退？             │
│                           │
│  撤退视为战败处理：         │
│  · 保留已获得的非绑定物品   │
│  · 已阵亡角色的绑定卡将失去 │
│                           │
│  [确认撤退]  [继续战斗]    │
└──────────────────────────┘
```

- 撤退按钮始终在战斗中可点击（位于右上角）
- 点击后弹出确认窗口
- 渡劫战中撤退按钮不可用（灰色+提示「天劫不可退」）

#### 11. 战斗日志

- 可收起的滚动文本日志（默认折叠为小标签）
- 点击展开为半屏覆盖，覆盖在手牌区上方
- 记录每次行动：抽牌、出牌、攻击、伤害、治疗效果、状态变更
- 日志条目格式：`[回合·阶段] 行动者 → 动作 → 目标 → 数值`
- 日志保留整场战斗的所有记录（退出战斗后清空）
- 日志可滚动查看（最新在最下方，自动滚到底部）

#### 12. 悬浮提示规则

| 元素 | 悬停显示内容 |
|------|------------|
| 手牌卡牌 | 放大版卡牌正面（效果全文、稀有度、类型、费用） |
| 场上己方角色 | 角色详情面板（HP/ATK/DEF、绑定卡效果、buff/debuff详情） |
| 场上敌方角色 | 敌方详情面板（HP/ATK、境界、buff/debuff、首目标标记） |
| 阵法 | 阵法效果详情（触发条件、效果描述、阵营条件） |
| 费用栏 | 费用来源明细（基础+临时） |
| 牌库计数 | 牌库剩余张数+鼠标滚轮浏览牌库内容 |
| 弃牌堆计数 | 弃牌堆张数+鼠标滚轮浏览弃牌堆内容 |
| 状态图标 | 状态名称+效果描述+剩余回合数 |

### 状态与转换

**战斗UI状态（与战斗系统状态同步）：**

| 状态 | 含义 | 交互可用性 |
|------|------|----------|
| **备战** | 战前角色选择 | 角色选择、前后排调整、确认开始 |
| **阶段0·准备** | 回合开始效果触发 | 不可操作（仅观看） |
| **阶段1·抽牌** | 抽牌动画播放 | 不可操作（仅观看） |
| **阶段2·出牌** | 玩家出牌阶段 | 手牌可拖拽、费用可见、阵法可操作 |
| **阶段3·布阵** | 玩家调整阵位 | 角色可拖拽移动、出牌不可用 |
| **阶段4·攻击声明** | 玩家选择攻击目标 | 角色可选目标、出牌不可用 |
| **阶段5·攻击结算** | 攻击动画播放 | 不可操作（仅观看） |
| **阶段6·敌方行动** | 敌方AI出牌+攻击 | 不可操作（仅观看） |
| **阶段7·结束** | 回合结束结算 | 不可操作（准备阶段开始前） |
| **胜利结算** | 战斗胜利 | 战利品三选一 |
| **战败结算** | 战斗战败 | 确认返回 |
| **撤退确认** | 撤退确认弹窗 | 确认/取消 |

### 与其他系统的交互

| 系统 | 数据流入（本系统→目标） | 数据流出（目标→本系统） |
|------|----------------------|---------------------|
| **战斗系统** | 玩家操作指令（出牌、选目标、布阵、撤退） | 战斗状态（阶段、角色状态、HP变更、费用变更） |
| **上场与阵位系统** | 出战角色选择、阵位移动操作 | 角色列表、阵位状态、待命/已就绪标记 |
| **卡牌系统** | 卡牌详情查询（悬浮预览） | 卡牌模板数据 |
| **费用系统** | — | 当前费用/上限/临时费用 |
| **卡牌效果引擎** | — | 效果触发时的视觉反馈请求 |
| **阵法系统** | — | 阵法部署状态 |
| **资源系统** | — | 战后灵石变更展示 |
| **探索系统** | 战斗结果（胜利/战败/撤退） | 战斗进入信号 |

## 公式

战斗UI系统不执行计算逻辑——所有数值计算由战斗系统和相关系统完成。UI只负责展示战斗系统提供的数据。

### 1. 手牌排列弧度

```
hand_card_position(index, total_cards, screen_width) → (x, y, rotation):
  arc_angle = min(30°, total_cards × 3°)  # 最多30°弧度
  base_y = screen_bottom - 100px
  spacing = min(card_width + 10px, screen_width / total_cards)
  x = screen_center + (index - (total_cards - 1) / 2) × spacing
  y = base_y - abs(x - screen_center) × tan(arc_angle / 2)
  rotation = (index - (total_cards - 1) / 2) × (arc_angle / total_cards)
  return (x, y, rotation)
```

| 变量 | 类型 | 范围 | 描述 |
|------|------|------|------|
| index | int | [0, total_cards-1] | 卡牌在手牌中的索引 |
| total_cards | int | [0, 10] | 当前手牌数量 |
| 返回值 | (float, float, float) | — | 卡牌位置(x, y)和旋转角度 |

**实现建议：** 使用 Godot 的 Container + 自定义布局脚本，按手牌数量动态调整间距和弧度。

### 2. HP条颜色阈值

```
hp_bar_color(hp_ratio) → Color:
  if hp_ratio > 0.6: return GREEN
  if hp_ratio > 0.3: return YELLOW
  return RED
```

| 变量 | 类型 | 范围 | 描述 |
|------|------|------|------|
| hp_ratio | float | [0.0, 1.0] | 当前HP / 最大HP |

### 3. 费用显示颜色

```
cost_color(current, max) → Color:
  ratio = current / max
  if ratio > 0.5: return WHITE
  if ratio > 0.0: return YELLOW
  return RED
```

## 边界情况

- **手牌超过屏幕宽度容纳**：手牌堆叠显示（每张卡牌部分重叠），玩家可拖拽滑动查看；最左侧和最右侧的卡牌露出手牌边缘指示可滚动
- **手牌为0**：手牌区显示「无手牌」提示文字，费用栏正常显示，不可出牌
- **场上角色全灭（战斗结束）**：立刻切入战败结算面板，不展示阵亡动画逐个播放
- **费用为0但手牌有0费卡**：0费卡正常彩色可出牌；费用>0的卡灰显
- **长时间悬浮卡牌**：悬浮卡牌预览保持打开直到鼠标移出卡牌区域，不自动关闭
- **同时声明多个角色攻击目标**：玩家逐一声明（点击角色→点击目标），已完成声明的角色有标记提示
- **攻击结算中玩家退出到桌面**：结算中断→下次读档回到战斗开始（战斗进度不保存）
- **宽屏/超宽屏适配**：手牌区自适应宽度，角色区居中不拉伸；两侧留黑边显示背景
- **低分辨率（1280×720）**：卡牌文字缩小，部分详情改为点击图标弹出查看，角色HP条缩短但不丢失功能
- **战斗中点击角色详情和手牌同时交互**：手牌区悬停显示卡牌详情和角色区悬停显示角色详情互不干扰
- **敌方角色过多超出屏幕**：敌方角色位自动缩小比例（最多6个角色，设计上可控）
- **临时费用用完消失**：费用栏中临时费用部分动画消失（绿色数字淡出），基础费用不变
- **结算面板中卡牌三选一与灵石选项**：卡牌和灵石都用卡片形式展示，卡牌显示正面信息，灵石显示图标+数量

## 依赖关系

| 依赖系统 | 性质 | 说明 |
|----------|------|------|
| **战斗系统** | 硬依赖 | 战斗状态数据、回合阶段信号、角色属性、HP变更 |
| **上场与阵位系统** | 硬依赖 | 战前角色选择、阵位状态、待命/已就绪标记 |
| **卡牌系统** | 硬依赖 | 卡牌模板数据（名称、费用、效果文字、稀有度颜色） |
| **费用系统** | 硬依赖 | 当前费用/上限/临时费用数据 |
| **卡牌效果引擎** | 软依赖 | 效果触发时的视觉反馈（非功能必需） |
| **阵法系统** | 软依赖 | 阵法部署状态展示 |
| **探索系统** | 软依赖 | 战斗结束信号（胜利/战败→返回探索） |

### 上游依赖设计顺序

战斗系统（已设计）→ 上场与阵位系统（已设计）→ 卡牌系统（已设计）→ 费用系统（已设计）→ **战斗UI系统**

## 调优参数

| 参数 | 默认值 | 安全范围 | 过低影响 | 过高影响 |
|------|:-----:|:--------:|---------|---------|
| 手牌弧形最大角度 | 30° | 15°~45° | 弧形太扁平不美观 | 太弯曲视野遮挡 |
| 手牌间距 | 卡牌宽+10px | 0~+30px | 太挤无法分辨多张卡 | 太宽超出屏幕 |
| 手牌悬停放大倍率 | ×1.2 | ×1.0~×1.5 | 放大不够看不清 | 放大太大挡视野 |
| 卡牌拖拽灵敏度 | 0.8（归一化） | 0.5~1.0 | 手感迟钝 | 可能误触 |
| HP条颜色阈值（绿→黄） | 60% | 40%~80% | 绿色区过大弱化危机感 | 黄色区过大过度警示 |
| HP条颜色阈值（黄→红） | 30% | 15%~50% | 红色区过小危机感不足 | 红色区过大麻木 |
| 战斗日志保留战斗 | 整场 | 最后50条~整场 | 回溯历史不足 | 内存占用（可优化） |
| 结算面板自动关闭延迟 | 无穷（需手动点） | 3~5s自动 | 手动操作多一步 | 玩家没看清就关了 |
| 备战选择动画时长 | 0.3s/角色 | 0.1s~0.5s | 太快没有确认感 | 太慢不耐烦 |
| 攻击结算动画最短时长 | 0.5s | 0.3s~1.0s | 太快看不清伤害 | 太慢拖节奏 |

## 视觉/音频需求

| 事件 | 动画 | 时长 |
|------|------|:----:|
| 回合阶段切换 | 阶段名称从左上角滑入+进度条切换 | 0.2s |
| 抽牌动画 | 卡牌从牌库区域飞入手牌区 | 0.3s |
| 出牌（手牌→场上） | 卡牌从手牌位置滑向目标/效果区域 | 0.3s |
| 费用消耗 | 费用栏数字跳动减少+光粒消散 | 0.2s |
| 伤害数字 | 目标位置飘出红色数字（向下掉落消失） | 0.5s |
| 治疗数字 | 目标位置飘出绿色数字（上浮消失） | 0.5s |
| 角色阵亡 | 角色卡碎裂→光点消散→阵位变空 | 0.5s |
| 待命→已就绪 | 角色头像沙漏标记消失+「待命」文字淡出 | 0.3s |
| 敌方行动（出牌） | 敌方手牌区小卡牌飞向目标区域 | 0.3s |
| 战斗胜利 | 全屏胜利文字缓缓浮现+战利品面板弹出 | 0.5s |
| 战斗失败 | 全屏灰暗过渡+失败文字浮现 | 0.5s |
| HP条变化 | HP条平滑增减（非瞬跳） | 0.3s |
| buff/debuff添加 | 图标从角色上方落入状态区 | 0.2s |
| 阵法部署 | 阵法卡从手牌飞向阵法区+光环扩展开 | 0.4s |

### 音效需求
- 抽牌：纸张滑出的声音
- 出牌：卡牌飞出的破空声（不同卡牌类型音调不同）
- 费用消耗：清脆的能量消耗音
- 伤害：命中声+数字伴随的嗖声
- 角色阵亡：低沉的消散音
- 回合阶段切换：轻快的提示音
- 战斗胜利：胜利号角
- 战斗失败：低沉的失败音

## 用户界面需求

| 界面 | 触发场景 | 核心功能 |
|------|----------|----------|
| **备战面板** | 战斗开始前 | 角色选择（选中/取消）、阵位布局预览、角色属性/绑定概览、确认/返回按钮 |
| **手牌区** | 战斗中(阶段2) | 弧形排列展示手牌、费用不足灰显、悬停放大预览、拖拽出牌交互 |
| **场上角色区** | 战斗中全程 | 角色头像+HP条+状态图标+绑定卡展示、前后排视觉区分、阵亡移除动画 |
| **费用栏** | 战斗中全程 | 当前费用/上限数字、临时费用展示（绿色）、牌库和弃牌堆计数 |
| **阵法区** | 战斗中全程 | 已部署阵法卡片展示、空位虚线框、悬停阵法效果详情 |
| **回合阶段指示器** | 战斗中全程 | 当前阶段名称+进度条、回合编号 |
| **攻击目标选择** | 阶段4 | 蓝色脉冲标记可攻击角色、红色高亮可选目标、锁定标记不可选目标、目标箭头连线 |
| **布阵交互** | 阶段3 | 角色可拖拽在前后排之间移动、移动后即时更新站位 |
| **战斗日志** | 战斗中全程（默认折叠） | 可收起的滚动日志、每次行动记录、展开时覆盖手牌区上方 |
| **战斗结算（胜利）** | 战斗胜利 | 三选一战利品面板、卡牌正面展示/灵石图标/消耗品 |
| **战斗结算（战败）** | 战斗战败 | 损失摘要（修为+角色+绑定卡）、确认返回按钮 |
| **撤退确认** | 玩家点击撤退 | 撤退后果说明、确认/取消按钮 |
| **悬浮详情面板** | 悬停卡牌/角色/阵法/状态图标 | 对应元素的详细信息展示面板 |

> **📌 用户体验标记—战斗UI系统**：此系统有复杂UI需求。在预生产中运行 `/ux-design` 为战斗HUD创建用户体验规格。

## 验收标准

- **GIVEN** 进入战斗，**WHEN** 加载完成，**THEN** 显示手牌区、场上角色区、费用栏、回合阶段指示器
- **GIVEN** 战斗开始（备战阶段），**WHEN** 查看界面，**THEN** 显示角色选择面板，可选角色高亮，不可用角色灰色+骷髅
- **GIVEN** 阶段2（出牌阶段），**WHEN** 查看手牌，**THEN** 费用充足的卡牌彩色，费用不足的灰显
- **GIVEN** 阶段2，**WHEN** 拖拽一张可出牌的卡牌到目标区域，**THEN** 卡牌跟随鼠标，释放后执行出牌
- **GIVEN** 阶段2之外（1/3/4/5/6/7），**WHEN** 尝试拖拽手牌，**THEN** 手牌灰显不可交互
- **GIVEN** 场上角色HP变化，**WHEN** 结算，**THEN** HP条平滑增减，数字更新
- **GIVEN** 场上角色HP<30%，**WHEN** 结算后，**THEN** HP条变为红色
- **GIVEN** 角色阵亡，**WHEN** HP归0，**THEN** 播放阵亡动画，角色移除，阵位变为空
- **GIVEN** 费用消耗，**WHEN** 出牌后，**THEN** 费用栏数字减少，播放消耗动画
- **GIVEN** 临时费用加入，**WHEN** 丹药/效果生效，**THEN** 费用栏显示绿色+数字
- **GIVEN** 阶段4（攻击声明），**WHEN** 点击己方角色，**THEN** 可选敌方目标高亮红色
- **GIVEN** 阶段4，**WHEN** 点击被前排保护的敌方后排，**THEN** 不可选，显示🔒标记
- **GIVEN** 战斗胜利，**WHEN** 结算，**THEN** 弹出三选一战利品面板
- **GIVEN** 战斗战败，**WHEN** 结算，**THEN** 弹出损失摘要面板
- **GIVEN** 战斗中点击撤退，**WHEN** 确认，**THEN** 弹出撤退确认窗口
- **GIVEN** 渡劫战中，**WHEN** 查看撤退按钮，**THEN** 按钮灰色不可用，提示「天劫不可退」
- **GIVEN** 鼠标悬停手牌卡牌，**WHEN** 超过0.3s，**THEN** 卡牌放大+显示详情
- **GIVEN** 鼠标悬停场上角色，**WHEN** 超过0.3s，**THEN** 显示角色详情面板
- **GIVEN** 鼠标悬停状态图标，**WHEN** 超过0.3s，**THEN** 显示状态名称+剩余回合
- **GIVEN** 阶段切换，**WHEN** 切换完成，**THEN** 阶段指示器更新名称和进度条

## 待解决问题

| # | 问题 | 影响 | 建议解决时间 |
|---|------|------|------------|
| 1 | 攻击目标选择的交互方式——点击式（点击己方→点击敌方）还是拖拽式（从己方拖到敌方）？当前设计为点击式，更精确；拖拽式更直觉但可能误触 | 交互手感 | 原型阶段测试 |
| 2 | 是否在战斗中显示敌方手牌数量？当前不显示（敌方手牌信息为隐藏信息），但可显示敌方牌库剩余数 | 战斗信息可见度 | UX设计时 |
| 3 | 手牌区是否支持键盘快捷键（如数字键1~7对应第X张手牌）？当前设计仅鼠标交互 | 便利性 | 原型阶段添加 |
| 4 | 结算面板的「三选一」在鼠标点选和键盘确认之间的交互——当前设计为点击即选无二次确认，如果误触怎么办？ | 操作容错 | 原型阶段测试 |
| 5 | 战斗日志的详细程度——是否显示具体数值（「造成5点伤害」）还是仅文字描述（「攻击了目标」）？当前设计显示数值 | 战斗信息 | UX设计时确认 |

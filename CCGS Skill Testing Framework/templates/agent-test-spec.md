
# 代理规格：[agent-name]

> **层级 (Tier)**：[directors | leads | specialists | godot | unity | unreal | operations | creative]
> **类别 (Category)**：[director | lead | specialist | engine | operations | creative]
> **规格编写日期 (Spec written)**：[YYYY-MM-DD]

## 代理概述

[一段描述此代理的领域、它拥有哪些决策权、以及它委派什么与直接处理什么。包括它触发的关卡（如有）。]

**领域 (Domain)**：[此代理拥有的文件/目录]
**升级至 (Escalates to)**：[父级代理 — 例如 design conflicts 升级至 creative-director]
**委派至 (Delegates to)**：[此代理通常生成的子代理]

---

## 静态断言

- [ ] 代理文件存在于 `.claude/agents/[name].md`
- [ ] 前置元数据包含 `name`、`description`、`model`、`tools` 字段
- [ ] 领域已明确说明
- [ ] 升级路径已记录
- [ ] 不在其领域之外做出决策

---

## 测试用例

### 用例 1：领域内请求 — [简要名称]

**场景：** 一个明确属于此代理领域的请求。

**测试夹具：**
- [相关项目状态]
- [提供给代理的输入]

**预期行为：**
1. 代理接受请求
2. 代理生成[特定输出类型]
3. 代理在写入文件前询问（如适用）

**断言：**
- [ ] 代理在其领域内处理请求，无需升级
- [ ] 输出格式符合预期结构
- [ ] 遵循协作协议（询问 → 草稿 → 批准）

**用例判定**：PASS / FAIL / PARTIAL

---

### 用例 2：领域外重定向 — [简要名称]

**场景：** 一个不属于此代理领域的请求。

**测试夹具：**
- [属于另一个代理的请求]

**预期行为：**
1. 代理识别出该请求不在其领域内
2. 代理重定向至正确的代理
3. 代理不尝试处理该请求

**断言：**
- [ ] 代理拒绝并重定向（不静默处理跨领域工作）
- [ ] 在重定向中指定了正确的代理名称

**用例判定**：PASS / FAIL / PARTIAL

---

### 用例 3：关卡判定 — [简要名称]

**场景：** 代理作为总监关卡检查的一部分被调用。

**测试夹具：**
- [用于评审的项目状态]
- [关卡 ID：例如 CD-PHASE-GATE]

**预期行为：**
1. 代理读取相关文档
2. 代理生成 PASS / CONCERNS / FAIL 判定
3. 代理在 CONCERNS 或 FAIL 时不会自动推进

**断言：**
- [ ] 输出中包含判定关键词（PASS、CONCERNS、FAIL）
- [ ] 为判定提供了推理说明
- [ ] 在 CONCERNS/FAIL 时：工作被阻塞，而非静默继续

**用例判定**：PASS / FAIL / PARTIAL

---

### 用例 4：冲突升级 — [简要名称]

**场景：** 此代理的领域与另一代理的决策发生冲突。

**测试夹具：**
- [来自同一层级两个代理的冲突决策]

**预期行为：**
1. 代理识别出冲突
2. 代理升级至共享父级（或 creative-director / technical-director）
3. 代理不单方面解决跨领域冲突

**断言：**
- [ ] 冲突被明确上报
- [ ] 遵循正确的升级路径
- [ ] 未做出单方面跨领域更改

**用例判定**：PASS / FAIL / PARTIAL

---

### 用例 5：上下文传递 — [简要名称]

**场景：** 代理从父级代理接收到带有完整上下文的任务。

**测试夹具：**
- [从父级传递的上下文块]
- [要执行的特定子任务]

**预期行为：**
1. 代理读取并使用提供的上下文
2. 代理完成子任务
3. 代理将结果返回给父级（不必要地不提示用户）

**断言：**
- [ ] 代理使用提供的上下文，而非重新询问
- [ ] 结果限定在子任务范围内，不超出其范围
- [ ] 输出格式适合父级代理消费

**用例判定**：PASS / FAIL / PARTIAL

---

## 协议合规性

- [ ] 停留在声明的领域内 — 无单方面跨领域更改
- [ ] 将冲突升级至正确的父级
- [ ] 在文件写入前使用"May I write"（或是只读的）
- [ ] 在请求批准前展示发现
- [ ] 在委派层级中不跳过层级

---

## 覆盖说明

[任何覆盖缺口、已知未测试的边缘情况、或需要实际调用代理来验证的行为。]
